name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: cd app && yarn
    - name: Run ESLint
      run: cd app && eslint . --ext .js,.ts,.vue